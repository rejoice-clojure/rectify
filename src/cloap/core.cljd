(ns cloap.core
  (:require
   [cloap.flutter.component-shim :as co]
   [cljd.flutter.alpha :as f]
   ["package:flutter/material.dart" :as fm])
  (:host-ns (:require [cloap.flutter.component :as co])))

(defmacro defc [& decls]
  (co/define-component decls))

(defc root []
  [:app
   [:txt "foo bar!!"]])

(defn main []
  (fm/runApp
   (root)))
